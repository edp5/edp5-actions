name: release.yml
on:
  workflow_dispatch:

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: edp5/edp5-actions/release@v1.1.0
        with:
          token: ${{ secrets.SERVICE_TOKEN }}
